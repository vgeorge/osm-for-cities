apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ofc-gitea-ebs-volume-clain
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: "gp2"
  resources:
    requests:
      storage: 20Gi
